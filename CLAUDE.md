# CLAUDE.md

The agent guidance for this repository lives in [`AGENTS.md`](AGENTS.md).

Read it before making changes. In short: read the docs first (especially
`docs/considerations.md`), every change ships with tests, log through
`AppLogger` at the right level, and keep the README/`docs` up to date.
