# The frontend talks to the backend through the Vite dev proxy (/api -> :8080),
# so no variables are strictly required for local development.
#
# Override the proxy target if the backend runs elsewhere:
# VITE_API_PROXY_TARGET=http://localhost:8080
